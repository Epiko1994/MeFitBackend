language: java
jdk:
  - openjdk17
services:
  - docker
install:
  - ./gradlew assemble
  - true
deploy:
  provider: script
  script:
    - ( echo "$heroku-email" echo "$heroku-password" ) | heroku container:login
    - heroku container:push -a mefitbackend-ajlm web
  on:
    branch: travisci